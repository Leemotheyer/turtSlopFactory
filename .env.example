# Optional overrides — copy to .env only if you need them.
# The factory works with zero env vars; configure hostname, API key, and Cursor in the dashboard.

# PUBLIC_HOST=192.168.1.50
# HTTP_PORT=8044
# IMAGE_TAG=latest
# API_KEY=           # or set in dashboard after deploy
# CURSOR_API_KEY=    # or connect in dashboard
